include(CMakeFindDependencyMacro)

if(EXISTS ${CMAKE_CURRENT_LIST_DIR}/@PROJ_NAME@Targets.cmake) # install tree
	
	include(${CMAKE_CURRENT_LIST_DIR}/@PROJ_NAME@Targets.cmake)
	list (APPEND CMAKE_MODULE_PATH ${CMAKE_CURRENT_LIST_DIR})

	if (TARGET Cork::cork) AND NOT ${GMP_FOUND} )
		find_dependency(GMP REQUIRED)
	endif()
	
endif()
